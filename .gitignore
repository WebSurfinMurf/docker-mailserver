
# refocus session index (regenerated)
docs/refocus/INDEX.md

# DKIM private key material - never commit
config/opendkim/
# Serena (MCP code tools) per-project state — local, not project knowledge (docs/context/ is)
.serena/
